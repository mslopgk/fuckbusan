# 전체 QA 보고서 — 2026-05-22

> Playwright + Figma MCP 기반 5개 병렬 에이전트 전수조사  
> Figma 원본: `jWpcqQv2jhb2mkzjEs1fuI` (메인), `TCuOzEqNhoLKjhF0reBDks` (신규)  
> 서버: Vite :8501 + FastAPI :8000 + MariaDB

---

## 📊 이슈 집계

| 우선순위 | 건수 | 설명 |
|---|---|---|
| 🔴 P0 — 즉시 수정 | 1 | 기능 완전 불능 버그 |
| 🟠 P1 — 중요 기능 | 7 | 핵심 기능 누락·실패 |
| 🟡 P2 — UI/디자인 | 22 | Figma 불일치, UX 미흡 |
| 🔵 INFO | 4 | 정책 결정 대기 |
| **합계** | **34** | |

---

## 🔴 P0 — 즉시 수정

### P0-1: 제안/제보 조회수 기록 500 에러
- **증상**: `POST /api/reports/proposals/{id}/view` → 500 Internal Server Error
- **원인**: `proposal_views` 테이블의 `user_id` FK constraint 위반 — admin 계정의 user_id(999999)가 `users` 테이블에 존재하지 않음
- **영향**: 모든 사용자에서 조회수 카운트 0 고정, 콘솔 500 에러 반복 발생
- **파일**: `backend/routers/report_router.py` — view tracking 엔드포인트
- **수정안**: view tracking 시 user_id 없으면 NULL 허용하거나, users 테이블에 anonymous/admin seed row 추가

---

## 🟠 P1 — 중요 기능 누락

### P1-1: ReportList.jsx — 백엔드 API 대신 하드코딩 mock 데이터 사용
- **증상**: 제보 리스트에 실제 백엔드 제보가 아닌 `src/data/mockReports.js`의 mock 데이터만 표시
- **영향**: 실제 사용자 제보가 목록에 보이지 않음
- **파일**: `src/components/ReportList.jsx`

### P1-2: ReportList.jsx — Leaflet 지도 사용 (Kakao Maps 아님)
- **증상**: 제보 리스트의 지도가 Kakao Maps SDK가 아닌 Leaflet으로 구현됨
- **영향**: 앱 전체 지도 라이브러리 불일치, Figma 디자인과 다름
- **파일**: `src/components/ReportList.jsx`

### P1-3: mReportMap 미구현
- **증상**: 모바일 제보 전용 지도 뷰(`mReportMap`)가 존재하지 않음
- **영향**: Figma `848:19015`의 전체화면 Kakao 지도+클러스터핀 화면 미구현
- **참고**: `mReportList`로 접근하면 hybrid list+소형지도 뷰만 있음

### P1-4: 진단 — 백엔드 target 파라미터 무시
- **증상**: `GET /checklist/list?target=citizen`과 `?target=expert` 모두 동일한 100건 반환
- **영향**: 시민/전문가 탭 필터링이 프론트엔드 클라이언트에서만 동작 (백엔드 미지원)
- **파일**: `backend/routers/checklist_router.py`

### P1-5: 설문 — MSurveyJoin Likert scale 질문 타입 JSX 미구현
- **증상**: `qtype === 'scale'` / `qtype === 'likert'` 질문이 CSS는 있으나 JSX 렌더링 핸들러가 없어 일반 체크박스로 표시됨
- **영향**: Figma의 슬라이더식 척도 질문이 체크박스로 degraded
- **파일**: `src/components/MSurveyJoin.jsx`

### P1-6: 설문 — MSurveyJoin 진행 표시 Figma 불일치
- **증상**: 코드=linear fill 바, Figma(`TCuOzEqNhoLKjhF0reBDks:0:11000`)=원형 dot indicator
- **상태**: TODO.md에 "정책 결정 대기"로 기록됨
- **파일**: `src/components/MSurveyJoin.jsx`

### P1-7: 진단 기본 구역 vs 시드데이터 불일치
- **증상**: 앱 기본 구역 = '부산진구', 시드데이터 district_code = '부산역' → 첫 로드 시 빈 목록
- **영향**: 신규 사용자는 진단 목록이 빈 상태로 보임
- **파일**: `src/components/MDiagnosisList.jsx` 기본값 또는 백엔드 시드 데이터

---

## 🟡 P2 — UI/디자인 이슈

### 제보 (Reports)

| # | 뷰 | 이슈 |
|---|---|---|
| P2-01 | reportDetail | 단계바 마지막 단계: 코드 "결과안내" vs Figma "처리완료" |
| P2-02 | reportPostForm | 카테고리 8개 vs Figma 4개 (known, TODO.md 정책결정 대기) |
| P2-03 | reportPostForm (PC) | PC 1440px에서 폼이 좌측 정렬 좁은 컬럼 — 반응형 미적용 |
| P2-04 | reportDetail | null author·lat·lng인 백엔드 레코드 → 작성자 공백, 지도 기본 위치 표시 |

### 제안 (Proposals)

| # | 뷰 | 이슈 |
|---|---|---|
| P2-05 | mProposalList | 카드에 작성자 지역 prefix 없음 (Figma: "동래구 우리디자이너") |
| P2-06 | mProposalList / pcProposeMap | 이미지 없는 제안 카드에 thumbnail placeholder 없음 |
| P2-07 | mProposalForm | 카테고리명 불일치: 코드 "산업·일자리" vs Figma "산업 및 고용" 등 |
| P2-08 | mProposalForm | 임시저장 버튼 스타일: 코드=pink border, Figma=grey background |
| P2-09 | mProposalDetail | 날짜 표시: 코드=절대날짜("2026. 5. 21."), Figma=상대날짜("어제") |
| P2-10 | mProposalDetail | 이미지 없는 제안: hero 이미지 placeholder 없음 (지도만 표시) |
| P2-11 | mProposalDone | 완료 아이콘 SVG 차이: 코드=flat document, Figma=scroll document |
| P2-12 | pcProposeMap | 이미지 없는 제안 모두 동일한 default 브릿지 사진 표시 |
| P2-13 | pcProposeForm | 390px 뷰포트에서 hero 텍스트 세로 줄바꿈 — PC 컴포넌트 viewport guard 없음 |

### 진단 (Diagnosis)

| # | 뷰 | 이슈 |
|---|---|---|
| P2-14 | mDiagnosisList | 빈 상태 문구: 로그인 상태에도 "로그인 후 전체 진단 내역을 볼 수 있습니다" 표시 |
| P2-15 | mDiagnosisForm | 섹션 라벨: 코드 "공공/시설물" vs Figma "분류" |
| P2-16 | mDiagnosisForm | 사진 첨부가 필수(`canSubmit` 조건)이나 required 표시 없음 |
| P2-17 | mDiagnosisResult | 제목 텍스트: 코드 "세부 정보**도** 확인해 보세요" vs Figma "세부 정보를 확인해 보세요" |
| P2-18 | mDiagnosisResult | Recharts container `width/height=-1` 경고 — chart 초기 렌더 시 크기 0 |
| P2-19 | pcDiagnosisMap | NaN LatLng 콘솔 에러 — null 좌표 checklist 레코드가 map에 도달 |

### 설문 (Survey)

| # | 뷰 | 이슈 |
|---|---|---|
| P2-20 | Backend | 중복 응답 방지 없음 — `survey_responses` 테이블에 (user_id, survey_id) unique constraint 없음 |
| P2-21 | pcSurveyResults | 데이터 2개 질문만 있을 때 2축 레이더 차트 렌더 (모바일엔 3축 미만 시 빈 배열 가드 있으나 PC엔 없음) |
| P2-22 | mSurveyDetail1 | 이용약관/개인정보 링크 클릭 시 `alert('준비 중')` — 실제 내용 없음 |

### 홈·마이페이지·AI가상시민·어드민

| # | 뷰 | 이슈 |
|---|---|---|
| P2-23 | reportManagement (admin) | 페이지 `<h2>` 제목 "제보" → "제보현황"으로 변경 필요 |
| P2-24 | mAICitizenDetail | citizen prop 없이 직접 접근 시 백지 화면 (no fallback UI, no back button) |
| P2-25 | pcAICitizen | 캐러셀에 "이전(←)" 버튼 없음 — 단방향 navigation |
| P2-26 | myPage | `.mypage-title` 색상 `#16B5B0` (admin teal) — 시민용 페이지에 어드민 브랜드색 |
| P2-27 | myActivity | `.page-title` 색상 `#E6235A` (pink=제보·제안) — 나의 활동 페이지에 부적합 |

---

## 🔵 INFO — 정책 결정 대기

| # | 항목 | 설명 |
|---|---|---|
| I-1 | MobileBottomNav | "가상시민" 탭이 설문이 아닌 `mAICitizen`으로 이동 — CLAUDE.md 스펙("설문" 탭)과 불일치. 의도적 변경이면 CLAUDE.md 갱신 필요 |
| I-2 | Home 통계 섹션 | `{false && ...}` 블록으로 숨겨진 상태 유지 — 활성화 여부 결정 대기 |
| I-3 | 임시저장 기능 | 진단·설문 임시저장 버튼이 모두 stub (alert) — 구현 일정 미정 |
| I-4 | 세부정보 버튼 | 진단 결과 3개 세부정보 버튼 모두 disabled — 기능 구현 미정 |

---

## ✅ 정상 확인 항목

- adminUserList 제목 "회원관리 - 관리자" (수정 완료 확인)
- adminMain disabled 카드 hover 시 teal 미적용 (CSS `:not(.disabled)` 정상)
- pcAICitizen 전체 16개 구 표시 (영도구·사하구·서구 포함)
- mAICitizen Leaflet scrollWheelZoom:false (스크롤 차단 수정 확인)
- MDiagnosisForm 7개 칩 전체 표시, 얼굴 이미지 1·3·5번 위치만
- MDiagnosisForm `submitting` 가드로 중복 제출 방지
- MSurveyJoin 필수 응답 없으면 제출 차단
- MSurveyDone 하단 네비 표시
- PCSurveyResults "자세히보기" toggle 정상 작동
- pcDiagnosisMap 우측 패널 카드 + teal 점수 표시
- mProposalDone / mReportDone Figma 텍스트 일치
- home 통계 섹션 `{false&&}` 숨김 유지

---

## 스크린샷 경로

```
verify/qa-screenshots/
  diagnosis/      — mDiagnosisList, mDiagnosisForm, mDiagnosisResult, pcDiagnosisMap
  proposal/       — mProposalList~Done, pcPropose*
  report/         — reportList(mobile+pc), reportPostForm, reportDetail, reportDone
  survey/         — mSurveyList~Results, pcSurveyList, pcSurveyResults
  misc/           — home-mobile, home-pc, myPage, myActivityHub, mAICitizen, pcAICitizen
  admin-*         — adminMain, adminUserList, expertManagement, reportManagement 등
```

---

## 수정 우선순위 권장 순서

1. **P0-1** `proposal_views` FK constraint → backend 즉시 수정
2. **P1-1·P1-2** ReportList mock data + Leaflet → 실제 API + Kakao Maps 교체
3. **P1-4** checklist target 파라미터 → backend 필터 추가
4. **P1-5** MSurveyJoin Likert scale JSX → 렌더러 구현
5. **P2-14** 진단 빈 상태 메시지 → 로그인/비로그인 분기
6. **P2-24** mAICitizenDetail fallback → redirect 또는 에러 UI
7. **P2-25** pcAICitizen 이전 버튼 추가
8. **P2-26·P2-27** myPage/myActivity 색상 수정
9. **P2-23** reportManagement 제목 수정
10. **P2-20** 중복 응답 DB unique constraint 추가
